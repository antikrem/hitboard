<UserControl x:Class="hitboard.InputBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:hitboard"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name = "Key" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="0" Click ="Key_Click"/>
        <Label x:Name = "Button" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1"/>

    </Grid>
</UserControl>
